已处理：把“File format”这个下拉框的最大高度调大，让你一打开就能看到全部选项（包括 Excel），不需要再滚动。改动在 import.vue （新增了 :menu-props="{ maxHeight: 600 }" ）。刷新页面即可生效。

以后你想加速排查/直接点名，我建议你按下面这份“页面/功能 → 代码位置”来指：

上传数据页（你截图这个）

- “File format”下拉框渲染与交互： import.vue
- 下拉框数据从哪来（拉 catalog）： import.vue （ this.$repositories.catalog.list(...) ）
- catalog 的前端请求封装： apiCatalogRepository.ts
- 后端 catalog 接口路由： urls.py （ /v1/projects/<id>/catalog ）
- 后端 catalog 返回内容（控制有哪些格式）： catalog.py （Options/filter）和各任务注册段落（比如 Excel 定义在 catalog.py ）
创建项目页 / “未认证”那类问题

- 创建项目页面与提交入口： create-project.vue
- 项目创建 API 调用链（service → repository → axios）： projectApplicationService.ts → apiProjectRepository.ts → api.service.js
- 登录态校验（每次进页面会不会重查 session）： check-auth.js + auth.js
- 401/403 统一处理（自动清理登录态+跳登录）： filters.js
如果你下次再遇到“某个下拉框/按钮/接口”问题，直接告诉我“发生在哪个页面 URL + 上面对应的文件链接”，我就可以跳过大范围搜索，直接在那个文件里改，速度会明显快很多。


################################################################################################################################################################

### 1. 📄 启动指南文档
我已在项目根目录下创建了 STARTUP_GUIDE.md 。
里面详细记录了 Backend、Frontend 和 Celery 三个服务的作用、端口以及手动启动的每一行命令，方便您随时查阅。

### 2. ⚡ 一键启动脚本 (推荐)
为了解放双手，我还写了一个 Powe下次您想开始工作时，只需要：

1. 找到 C:\Users\mlian\doccano\dev_start.ps1 这个文件。
2. 右键 点击它 -> 选择 “使用 PowerShell 运行” 。
以后每次开发，您只需要做这一件事：



  【 “知识纠错 (测试)” 】 ###################################################################################################################################################################
   没问题。我非常理解这种“既要尝试新功能，又要随时准备无痕回退”的需求。

我为您在“创建项目”页面增加了一个独立的 “知识纠错 (测试)” 卡片。

### 实现方案特点：
1. 独立入口 ：在现有的项目类型列表后，追加了一个名为“知识纠错 (测试)”的卡片。
2. 零侵入后端 ：这个新入口在底层直接复用了您刚刚升级过的“序列标注”功能（SequenceLabeling），因此不需要修改数据库或后端逻辑，风险极低。
3. 便于删除 ：所有的相关代码（前端模板和逻辑）我都用 [EXPERIMENTAL-FEATURE] 标签进行了显式包裹。如果未来需要砍掉这个功能，只需要搜索这个标签并删除对应代码块，即可瞬间恢复原状，不留任何痕迹。
### 变更预览：


### 🛡️ 承诺的一键清理
如果后期需要“砍掉”这个功能，只需三步：

1. 搜索 ：全局搜索 [EXPERIMENTAL-FEATURE] 。
2. 删除 ：删除被 START 和 END 标记包裹的代码块。
3. 恢复 ：对于像 i18n 这种单行修改，直接根据注释恢复原样即可。
功能已经全部就绪，您可以放心在界面上创建“纠错”标签来体验划选、弹窗、改错的一体化流程了！、
### 🛡️ 标记与清理机制 (Ready for Rollback)
我已经更新了所有相关文件中的标记，确保“砍掉”功能时可以一键清理。所有改动均被以下标记包裹：

- // [EXPERIMENTAL-FEATURE-START]
- // [EXPERIMENTAL-FEATURE-END]

### 🛡️ 代码记号 (Rollback Ready)
我已经在所有新增的 UI 代码块前后都打好了记号：

- <!-- [EXPERIMENTAL-FEATURE-START] Knowledge Correction Display -->
- <!-- [EXPERIMENTAL-FEATURE-END] -->