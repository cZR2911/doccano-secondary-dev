【1.30改动记录】_2026-01-30

1. **新增实验性项目类型**
   - 在创建项目页面增加“知识纠错 (测试)”卡片。
   - 该类型对应后端的 SequenceLabeling 项目类型，但在前端进行了特殊标记 `[EXPERIMENTAL-FEATURE]` 以便识别和回滚。

2. **Excel 导入功能增强**
   - 支持导入 `comment` (批注) 和 `correction` (纠错) 列。
   - 兼容中文列名 `批注` 和 `纠错`。
   - 导入的数据存储在 Example 的 comments 字段中，支持后续的知识纠错流程。

3. **前端界面调整**
   - 将标注界面的“Comments”翻译调整为“批注与纠错”，以符合业务场景。

4. **开发环境配置**
   - 确认本地开发环境配置：后端使用 Conda `doccano` 环境，前端使用 Node.js LTS。
   - 解决 marshmallow 和 numpy 版本兼容性问题。
